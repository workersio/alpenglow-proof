\* Configuration file for ShredTest.tla specification

SPECIFICATION Spec

\* Define finite sets for model checking (smaller sets for testing)
CONSTANTS
    Slots = {1}
    SliceIndices = {1, 2}
    ShredIndices = {1, 2, 3}
    MerkleRoots = {"root1", "root2"}
    ShredData = {"data1", "data2", "data3"}
    MerklePaths = {"path1", "path2", "path3"}
    Signatures = {"sig1", "sig2", "sig3"}
    Nodes = {"node1", "node2"}
    UDP_MAX_SIZE = 1500

\* Test-specific invariants
INVARIANTS
    TypeOK
    TestAssertions

\* Properties to verify test scenarios
\* PROPERTIES

\* Constraint to limit search space for testing
CONSTRAINT TRUE