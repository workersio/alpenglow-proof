\* Configuration file for Shred.tla specification

SPECIFICATION Spec

\* Define finite sets for model checking
CONSTANTS
    Slots = {1, 2}
    SliceIndices = {1}
    ShredIndices = {1, 2}
    MerkleRoots = {"root1"}
    ShredData = {"data1", "data2"}
    MerklePaths = {"path1"}
    Signatures = {"sig1"}
    Nodes = {"node1", "node2"}
    UDP_MAX_SIZE = 1500
    GAMMA = 2

\* Invariants to check
INVARIANTS
    TypeOK
    ValidShredPreservation
    NoDuplicatePositions
    CorrectLeaderSignatures
    MerklePathConsistency
    UDPSizeConstraint
    ShredDataIntegrity

\* Properties to check (commented out for basic model checking)
\* PROPERTIES
\*     []ValidShredPreservation
\*     []NoDuplicatePositions

\* State constraints to limit search space
CONSTRAINT StateConstraint