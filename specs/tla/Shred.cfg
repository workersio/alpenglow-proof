\* Configuration file for Shred.tla specification

SPECIFICATION Spec

\* Define finite sets for model checking
CONSTANTS
    Slots = {1, 2, 3}
    SliceIndices = {1, 2}
    ShredIndices = {1, 2, 3, 4}
    MerkleRoots = {"root1", "root2", "root3"}
    ShredData = {"data1", "data2", "data3"}
    MerklePaths = {"path1", "path2", "path3"}
    Signatures = {"sig1", "sig2", "sig3"}
    Nodes = {"node1", "node2", "node3"}
    UDP_MAX_SIZE = 1500

\* Invariants to check
INVARIANTS
    TypeOK
    ValidShredPreservation
    NoDuplicatePositions
    CorrectLeaderSignatures
    MerklePathConsistency
    UDPSizeConstraint
    ShredDataIntegrity

\* Properties to check (commented out for basic model checking)
\* PROPERTIES
\*     []ValidShredPreservation
\*     []NoDuplicatePositions

\* State constraints to limit search space
\* CONSTRAINT Cardinality(shreds) <= 10